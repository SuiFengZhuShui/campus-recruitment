# 校园招聘平台

连接学校、学院、企业与学生的招聘平台。企业发布岗位，学生投递，学院与学校层做审核与统计。

## Language

### 权限层次

**认证 (Authentication)**:
确认请求来自一个已登录的 User。
_Avoid_: 登录校验、身份验证（"身份"在本项目另有含义：Student 的在校资料）

**角色门禁 (Role gating)**:
判定这个已登录的 User 是否属于允许访问该端点的主体类型。
_Avoid_: 权限检查、鉴权（两者都与数据归属混淆）

**数据归属 (Data ownership)**:
判定某条具体业务记录是否归当前 User 所有。
_Avoid_: 数据权限、越权检查

**账号状态门禁 (Account status gating)**:
判定主体自身的审核状态是否允许它执行该动作。例如 Enterprise 未过审不得发布岗位。
_Avoid_: 权限、审核检查

### 主体与组织

**主体类型 (Subject type)**:
User 作为哪一类行为主体：Enterprise（单位）或 Student（个人）。二者互斥，一个 User 只能是其一。
_Avoid_: 角色、账号类型

**组织层级 (Organization tier)**:
User 处于哪一层组织：School（学校）或 College（学院）。School 包含 College，是一对多的包含关系。
_Avoid_: 角色、单位、部门

**School（学校）**:
平台的部署单元。当前部署只存在一个 School。
_Avoid_: 校区、大学、总校

### Flagged ambiguities

**"权限"** 在本项目被用于指代上述四层中的任意一层，是本仓库最容易造成误解的词。讨论前必须先指明是哪一层：认证 / 角色门禁 / 数据归属 / 账号状态门禁。

**`role` 字段** 同时承载主体类型与组织层级两个维度，而字段名暗示只有一维。四个值并不等价：`school` / `college` 是组织层级，`enterprise` / `student` 是主体类型。把 `role` 当作单一维度推理会出错。

## Example dialogue

**开发者**：企业那几个接口得加个权限。

**领域专家**：哪一层？没登录是**认证**；学生账号能访问企业专属接口，那是**角色门禁**；要改动某一条具体的岗位，那是**数据归属**；企业没交材料没过审就不准发岗位，那是**账号状态门禁**。

**开发者**：那"企业只能看自己发布的岗位"呢？

**领域专家**：那是两层叠加——先看是不是企业（角色门禁），再看这条岗位是不是它的（数据归属）。少一层都能穿。
