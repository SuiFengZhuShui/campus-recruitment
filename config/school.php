<?php

/**
 * 学校品牌配置 — 模板文件，其他学校部署时修改此处即可
 *
 * 使用：config('school.name') / config('school.logo') 等
 */
return [

    /*
    |--------------------------------------------------------------------------
    | 学校基本信息
    |--------------------------------------------------------------------------
    */
    'name'       => env('SCHOOL_NAME',       '校园招聘平台'),
    'short_name' => env('SCHOOL_SHORT_NAME', '校园招聘'),
    'domain'     => env('SCHOOL_DOMAIN',     'example.com'),

    /*
    |--------------------------------------------------------------------------
    | 平台名称（显示在学校名下方或旁边）
    |--------------------------------------------------------------------------
    */
    'platform_name' => env('PLATFORM_NAME', '实习生线上招聘平台'),

    /*
    |--------------------------------------------------------------------------
    | 校徽 / Logo
    |--------------------------------------------------------------------------
    | 'logo' — 校徽图片路径（public/ 下），建议 PNG/SVG，高度自动缩放至 40px
    | 留空则使用纯文字校名（无校徽）
    */
    'logo' => env('SCHOOL_LOGO', 'images/school-logo.svg'),

    /*
    |--------------------------------------------------------------------------
    | 页面标题格式
    |--------------------------------------------------------------------------
    | {school} → 学校全称，{platform} → 平台名称
    */
    'title_format' => '{platform} — {school}',

    /*
    |--------------------------------------------------------------------------
    | Footer 信息
    |--------------------------------------------------------------------------
    */
    'copyright'  => '© ' . date('Y') . ' ' . env('SCHOOL_NAME', '校园招聘平台') . ' 版权所有',
    'address'    => '',
    'icp'        => '',

];
